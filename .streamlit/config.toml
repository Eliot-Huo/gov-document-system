[server]
fileWatcherType = "none"
runOnSave = false

[client]
showErrorDetails = true
```

5. **Commit new file**

---

### 方法 2：等待 Streamlit Cloud 自動恢復

這個錯誤有時候會自己消失（伺服器重新分配資源）。

---

## 🎯 現在請做：

### 立即執行：建立 config.toml

1. 前往：https://github.com/Eliot-Huo/gov-document-system
2. 點擊「Add file」→「Create new file」
3. 在檔名欄位輸入：`.streamlit/config.toml`
   - 注意：輸入 `/` 會自動建立資料夾
4. 貼上上面的內容
5. Commit

**等待 1-2 分鐘自動重新部署**

---

## 📊 預期結果

建立 `config.toml` 後：
```
✅ 禁用檔案監控
✅ 避免 inotify 錯誤
✅ 系統應該能正常啟動
