[server]
fileWatcherType = "none"
runOnSave = false
```

### 步驟 3：Commit changes

---

## 🎯 如果還是出錯

### 方案 A：手動輸入（不要複製貼上）

1. 刪除所有內容
2. **手動打字**輸入：
```
[server]
fileWatcherType = "none"
runOnSave = false
